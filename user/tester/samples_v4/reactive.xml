<?xml version="1.0" encoding="UTF-8"?>
<root BTCPP_format="4">
  <BehaviorTree ID="MoveReactive">
    <ReactiveSequence _description="If the 2nd child returns RUNNING,
the ReactiveSequence restarts from the 1st child.">
      <CheckBattery _description="This will be called periodically and may halt MoveBase"/>
      <Sequence>
        <SaySomething message="mission started"/>
        <MoveBase goal="1;2;0.5"/>
        <SaySomething message="mission started"/>
      </Sequence>
    </ReactiveSequence>
  </BehaviorTree>

  <BehaviorTree ID="MoveSimple">
    <Sequence>
      <CheckBattery _description="This is called only once, before starting MoveBase"/>
      <SaySomething message="mission started"/>
      <MoveBase goal="1;2;0.5"/>
      <SaySomething message="mission completed"/>
    </Sequence>
  </BehaviorTree>

  <!-- Description of Node Models (used by Groot) -->
  <TreeNodesModel>
    <Condition ID="CheckBattery"
               editable="true"/>
    <Action ID="MoveBase"
            editable="true">
      <input_port name="goal"/>
    </Action>
    <Action ID="SaySomething"
            editable="true">
      <input_port name="message"/>
    </Action>
  </TreeNodesModel>

</root>
